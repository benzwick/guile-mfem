name: testrelease_binary

on:
  push:
    tags: ["test_*"]

jobs:
  test-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      # TODO: Build guile-mfem shared libraries and create test release artifacts
      - name: Build test release
        run: |
          echo "TODO: build guile-mfem test release binaries"
          exit 1
